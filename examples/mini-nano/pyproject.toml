[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mini-nano"
version = "0.1.0"
description = "Teaching-sized Nano agent harness"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
mini-nano = "mini_nano.cli:main"

[dependency-groups]
dev = [
  "pytest>=8.3.5",
  "ruff>=0.4.4",
]

[tool.setuptools.packages.find]
include = ["mini_nano*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
